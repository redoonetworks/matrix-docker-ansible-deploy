---
kind: Namespace
apiVersion: v1
metadata:
  name: {{ matrix_k8s_namespace }}
{% if matrix_k8s_namespace_labels is defined and matrix_k8s_namespace_labels|length > 0 %}  
  labels:
    {{ matrix_k8s_namespace_labels|to_nice_yaml(indent=4)|trim|indent(4) }}
{%- endif %}  