{{- $secretName := (printf "%s-synapse-secret" .Release.Name) }}
---
apiVersion: v1
kind: Secret
metadata:
  annotations:
    helm.sh/resource-policy: keep
  name: {{ $secretName }}
  labels:
    app.kubernetes.io/component: signingkey-job
{{ include "deployment.selector" . | indent 4 }}

